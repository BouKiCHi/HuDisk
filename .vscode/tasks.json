{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make",
            "command": "make",
            "type": "process",
            "options": {
                "cwd": "${workspaceFolder}/src"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}